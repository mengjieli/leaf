namespace tiny2d {

    export class Utils {

        static sin(r:number) {
            return Math.sin(r);
        }

        static cos(r:number) {
            return Math.cos(r);
        }

        static atan2(y: number, x: number) {
            return Math.atan2(y, x);
        }

    }

}